import WSConnect from '../WSConnect';

describe('WSConnect.settings', () => {
    /*
    const mockSettings = {};
    const mockOptions = {};
    beforeEach(() => {});
*/
    it('description', () => {
        const result = null;
        expect(result).toEqual(null);
    });
});

describe('WSConnect.eventCallbacks', () => {
    /*
        const mockSettings = {};
        const mockOptions = {};
        beforeEach(() => {});
    */
    it('description', () => {
        const result = null;
        expect(result).toEqual(null);
    });
});

describe('WSConnect.constructor', () => {
    /*
        const mockSettings = {};
        const mockOptions = {};
        beforeEach(() => {});
    */
    it('description', () => {
        const result = null;
        expect(result).toEqual(null);
    });
});

describe('WSConnect.init', () => {
    /*
        const mockSettings = {};
        const mockOptions = {};
        beforeEach(() => {});
    */
    it('description', () => {
        const result = null;
        expect(result).toEqual(null);
    });
});

describe('WSConnect.createWebSocketConnection', () => {
    /*
        const mockSettings = {};
        const mockOptions = {};
        beforeEach(() => {});
    */
    it('description', () => {
        const result = null;
        expect(result).toEqual(null);
    });
});

describe('WSConnect.exit', () => {
    /*
        const mockSettings = {};
        const mockOptions = {};
        beforeEach(() => {});
    */
    it('description', () => {
        const result = null;
        expect(result).toEqual(null);
    });
});

