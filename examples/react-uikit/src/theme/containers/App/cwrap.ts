import { IBlocks } from "../../../definitions";

export const blocks: IBlocks = {
   core: {
      minHeight: '100vh',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
   },
   mobile: {},
   laptop: {},
   desktop: {},
};

export default blocks;