export const initialLogs = {
    enabled: false,
    mode: 'PROD',
    logFn: console.log,
};

export const testLogs = {
    enabled: true,
    mode: 'DEV',
    logFn: console.log,
};

export default initialLogs;
