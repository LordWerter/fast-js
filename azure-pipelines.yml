jobs:
- job: Linux
  trigger:
  - main
  pool:
    vmImage: 'ubuntu-latest'
  steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '14.x'
    displayName: 'Install NodeJS'
  - script: echo NodeJS was installed
  - script: yarn install
    displayName: 'yarn install'
