import Slide from './Slide';

describe('Slide Class', () => {

    let domComposition = null;

    beforeEach(() => {
        domComposition = null;
    });

    it('Check of build DOM with Initial Settings', () => {
        domComposition = new Slide();

        // checking for the Main Wrapper Node was created for slider

    });
});
